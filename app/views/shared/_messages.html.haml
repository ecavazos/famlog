%ul#messages
  - @messages.each do |m|
    %li(class="#{type_name_to_css(m)}")
      .action
        = to_edit(m)
      .message-body
        %h3
          = m.title
        %p
          = link_to m.user.username
          says:
          = m.message

      - event_info(m)

      .message-meta
        %strong(class="#{to_css(m.importance)}")
          = m.importance
        \-
        = format_datetime(m.created_at)
        \-
        = link_to "Delete", message_path(m), :method => :delete
