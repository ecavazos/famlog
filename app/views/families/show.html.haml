%h2
  = @family.name
  Family

%ul.members
  - @family.users.each do |u|
    %li
      = u.fullname
      &mdash;
      = link_to 'Delete', family_member_path(u), :method => :delete, :confirm => 'Are you sure?'

%div
  = link_to 'New Family Member', new_family_member_path()
