- form_for @message do |f|
  %p
    = f.label :message
    = f.text_area :message, :cols => 77, :rows => 3
  %p
    = f.label :importance
    = f.select :importance, Importance::ALL
  %p
    = f.submit "Submit", :class => "submit"
    or
    = link_to "Cancel", root_path, :class => "cancel"
